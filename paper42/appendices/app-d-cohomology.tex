\section{Cohomology representatives}

Let

\[
Y=L(P).
\]

The basic dimensions are

\[
|V(Y)|=15,
\qquad
|E(Y)|=30,
\]

\[
\dim Z_1(Y,\Ftwo)=16,
\]

and

\[
\dim H^1(Y,\Ftwo)=16.
\]

The certified invariant calculation gives

\[
\dim H^1(Y,\Ftwo)^{S_5}=2.
\]

The triangle span has rank ten. The triangle span together with the
certified pentagon orbit has rank sixteen.

The four fixed classes have coordinates

\[
\begin{array}{c|c}
\text{Class} & (t,p)\\
\hline
\mathcal C_{00} & (0,0)\\
\mathcal C_{01} & (0,1)\\
\mathcal C_{10} & (1,0)\\
\mathcal C_{11} & (1,1)
\end{array}
\]

and satisfy

\[
\mathcal C_{01}+\mathcal C_{10}
=
\mathcal C_{11}.
\]

The graph \(X\) is the derived graph of \(\mathcal C_{11}\).

% STATUS: Review-draft insertion boundary

The final appendix must include:

\begin{itemize}

\item the exact edge ordering of \(Y\);

\item the coboundary matrix;

\item a basis for the two-dimensional fixed subspace;

\item the selected triangle and pentagon test cycles;

\item the four voltage vectors; and

\item the certificate hashes.

\end{itemize}

These data are available in the Project 41 cohomology-square packet and
must be imported without manual re-entry.
