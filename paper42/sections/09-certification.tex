\section{Computational certification and reproducibility}

The conceptual arguments in this paper are combined with exact finite
computations. This section records which statements depend on
certificates and which statements are deductions from them.

\subsection{Labeled construction}

All computations begin from one fixed labeled Petersen graph. Its edges
label the vertices of

\[
Y=L(P),
\]

and the vertices of the canonical bipartite double cover are labeled by

\[
V(Y)\times\Ftwo.
\]

Appendix~A gives both the coordinate-free labeled model and the exact
conversion to the integer labels used in the machine-readable
certificates. Certificate~\(045\) records the deterministic bridge from
the standard integer-labeled Petersen model to the fixed \(KG(5,2)\)
coordinates. No mathematical claim depends on this editorial coordinate
choice.

\subsection{Certificate ledger}

The original Project 42 evidence baseline is repository commit

\[
\texttt{1604b16}.
\]

The present revision imports the authoritative Project 41 packet without
modification and adds deterministic Project 42 certificates \(045\)
through \(048\). Appendix~F records their exact paths and SHA-256
digests. The release commit and archive digest are recorded by the
external release manifest generated after packaging.

The principal certified inputs are summarized in
Table~\ref{tab:certificate-ledger}.

\begin{table}[h]
\centering
\begin{tabularx}{\textwidth}{lXX}
\toprule
Result & Evidence identifier & Role in the paper\\
\midrule

Labeled coordinate bridge
&
\texttt{project42\_kneser\_label\_bridge\_045.json}
&
Records the deterministic conversion between the integer Petersen model
and the fixed \(KG(5,2)\) coordinates used in Appendix~A.\\

Full automorphism order and group anatomy
&
\texttt{project42\_group\_orbital\_certificate\_046.json}
&
Combines the exact \(720\)-element carrier action with the certified
\(S_5\times S_3\) structure, center, derived subgroup, abelianization,
and deterministic permutation generators.\\

Quartic orbital census
&
\texttt{project42\_group\_orbital\_certificate\_046.json}
&
Establishes four quartic self-paired orbitals, with exactly one connected,
bipartite, triangle-free member.\\

Cover-orbital identification
&
\texttt{project42\_group\_orbital\_certificate\_046.json}
&
Matches the selected connected orbital edge set exactly with the all-one
canonical double cover.\\

Deck-centralizer alignment
&
\texttt{project42\_quotient\_frame\_orbit\_certificate\_004.json}
&
Places the natural deck involution inside the complementary \(S_3\).\\

Three frame partitions and pair census
&
\texttt{project42\_quotient\_frame\_orbit\_certificate\_004.json}
&
Records the three involutions, their fifteen-pair orbit partitions, and
the forty-five distinct unordered pairs.\\

Fixed cohomology square
&
\texttt{project42\_cohomology\_certificate\_047.json}
&
Reconstructs
\(H^1(L(P),\Ftwo)^{S_5}\cong\Ftwo^2\), the coboundary matrix, fixed basis,
triangle-pentagon coordinates, and four voltage representatives.\\

Four invariant cover classes
&
\texttt{project42\_cover\_class\_certificate\_048.json}
&
Records exact graph fingerprints, canonical edge digests, voltage
representatives, and automorphism orders for all four classes.\\

\bottomrule
\end{tabularx}
\caption{Principal computational inputs. Exact repository paths and
digests are normalized in Appendix~F.}
\label{tab:certificate-ledger}
\end{table}

The path-and-digest ledger is generated directly from repository files
and printed in Appendix~F. The preserved
\texttt{notes/source-ledger.md} contains no historical digest entries and
is not used as a substitute for filesystem-derived hashes.

\subsection{Conceptual deductions}

The following results are not separate searches:

\begin{itemize}

\item
The centralizer formula

\[
C_{\Sym(S_5/H)}(S_5)\cong N_{S_5}(H)/H
\]

is proved conceptually.

\item
The complementary \(S_3\) is obtained from the normalizer quotient and the
certified uniqueness of the connected quartic orbital relation.

\item
The subgroup

\[
S_5\times S_3
\]

is constructed conceptually. The automorphism enumeration is used only
as the matching upper bound.

\item
The three quotient graphs and their stabilizers follow by conjugacy after
the single certified alignment of \(\tau_0\) with the complementary
factor.

\item
The forty-five-pair count follows conceptually from semiregularity of the
centralizer action, although the explicit labeled census remains an
independent check.

\item
The center, derived subgroup, and abelianization follow directly from the
direct-product theorem.

\end{itemize}

\subsection{Current nonclaims}

The paper does not currently claim:

\begin{itemize}

\item that every regular \(L(P)\)-quotient frame of \(X\) belongs to
\(\frorbit\);

\item a conceptual classification of the four quartic double cosets;

\item a representation-theoretic derivation of the fixed cohomology basis;

\item a standard external graph name for \(X\); or

\item that multiple quotient frames characterize the all-one cover class
among all invariant covers.

\end{itemize}

\subsection{Reproduction contract}

A complete reproduction release must contain:

\begin{enumerate}

\item the labeled definitions of \(P\), \(Y\), and \(X\);

\item the permutation generators for the transitive \(S_5\)-action;

\item the quartic orbital census and graph fingerprints;

\item the explicit graph isomorphism certificate;

\item the full automorphism generators and order certificate;

\item the three quotient involutions and pair partitions;

\item the fixed-cohomology basis and four voltage representatives;

\item the software and package versions;

\item the repository commit; and

\item SHA-256 digests for every source and output artifact.

\end{enumerate}

The long-form reproduction manifest belongs in Appendix~F rather than in
the main mathematical argument.
